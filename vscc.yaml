name: my-express-app
language: node
properties:
  public: true
configurations:
  develop:
    command: ["npm", "run", "watch"]
    public: false
    watch:
      package.json:
        action: rebuild
  develop.debug:
    ports:
      debug: 9229:9229
    command: ["npm", "run", "debug", "--", "--inspect=0.0.0.0:9229"]
  release:
    sync: false
