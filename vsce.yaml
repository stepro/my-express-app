name: my-express-app
version: 1.0.0
language: node
properties:
  public: true
configurations:
  develop:
    command: ["npm", "run", "develop"]
    public: false
    watch:
      package.json:
        action: restart
  develop.vscode:
    ports:
      debugger: 9229:9229
    command: ["npm", "run", "vsce-develop"]
  release:
    sync: false
